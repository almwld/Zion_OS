include ':app'

// إضافة مجلدات Termux كمشاريع فرعية
include ':terminal-emulator'
project(':terminal-emulator').projectDir = new File(rootProject.projectDir, '../lib/termux/terminal-emulator')

include ':terminal-view'
project(':terminal-view').projectDir = new File(rootProject.projectDir, '../lib/termux/terminal-view')

include ':termux-shared'
project(':termux-shared').projectDir = new File(rootProject.projectDir, '../lib/termux/termux-shared')
